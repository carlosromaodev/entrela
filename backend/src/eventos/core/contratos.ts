import type{Evento}from'./modelo.js';export interface RepositorioDeEventos{papel(n:string,u:string):Promise<'PROPRIETARIO'|'ADMINISTRADOR'|'EDITOR'|null>;criar(e:{id:string;negocioId:string;criadorId:string;dados:Evento}):Promise<void>;editar(e:{id:string;negocioId:string;dados:Partial<Evento>}):Promise<boolean>;publicar(e:{id:string;negocioId:string;agora:Date}):Promise<boolean>;obter(id:string,negocioId:string):Promise<(Evento&{estado:'RASCUNHO'|'PUBLICADO'|'CANCELADO'})|null>}
