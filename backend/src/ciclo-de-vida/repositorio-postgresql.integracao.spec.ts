import{randomUUID}from'node:crypto';import{Pool}from'pg';import{afterAll,beforeAll,describe,expect,it}from'vitest';import{RepositorioDoCicloDeVidaPostgresql}from'./repositorio-postgresql.js';const url=process.env.URL_DE_BASE_DE_DADOS_DE_INTEGRACAO
describe.skipIf(!url)('correcção PostgreSQL 18',()=>{const p=new Pool({connectionString:url}),s=`ciclo_${randomUUID().replaceAll('-','')}`,n=randomUUID(),u=randomUUID(),e=randomUUID(),v=randomUUID(),porta=randomUUID();beforeAll(async()=>{await p.query(`CREATE SCHEMA "${s}";CREATE TABLE "${s}".membros_do_negocio(negocio_id uuid,utilizador_id uuid,papel text,estado text);CREATE TABLE "${s}".versoes_da_experiencia(id uuid primary key,experiencia_id uuid,numero int,estado text,criado_por_utilizador_id uuid,publicada_em timestamptz,soma_de_verificacao text);CREATE TABLE "${s}".experiencias(id uuid primary key,negocio_id uuid,estado text,versao_publicada_id uuid,versao_de_rascunho_atual_id uuid,atualizado_em timestamptz);CREATE TABLE "${s}".traducoes_da_experiencia(versao_da_experiencia_id uuid,idioma text,titulo text,resumo text,metadados_de_partilha jsonb);CREATE TABLE "${s}".blocos(id uuid primary key,versao_da_experiencia_id uuid,chave_do_bloco text,tipo text,posicao int,configuracao jsonb,versao_do_esquema int,visibilidade_inicial text);CREATE TABLE "${s}".traducoes_do_bloco(bloco_id uuid,idioma text,conteudo jsonb);CREATE TABLE "${s}".politicas_de_disponibilidade(versao_da_experiencia_id uuid,abre_em timestamptz,expira_em timestamptz,fuso_horario text,modo text);CREATE TABLE "${s}".regras(id uuid,versao_da_experiencia_id uuid,chave_da_regra text,condicoes jsonb,acoes jsonb,estado text,prioridade int,tipo_de_gatilho text,versao_do_esquema int);CREATE TABLE "${s}".pontos_de_acesso(id uuid,experiencia_id uuid,versao_da_experiencia_id uuid,estado text);INSERT INTO "${s}".membros_do_negocio VALUES('${n}','${u}','PROPRIETARIO','ATIVO');INSERT INTO "${s}".versoes_da_experiencia VALUES('${v}','${e}',1,'PUBLICADA','${u}',now(),'soma');INSERT INTO "${s}".experiencias VALUES('${e}','${n}','PUBLICADA','${v}',null,now());INSERT INTO "${s}".traducoes_da_experiencia VALUES('${v}','pt-AO','Título','Resumo','{}');INSERT INTO "${s}".blocos VALUES('${randomUUID()}','${v}','final','ETAPA',1,'{}',1,'VISIVEL');INSERT INTO "${s}".politicas_de_disponibilidade VALUES('${v}',now(),null,'Africa/Luanda','IMEDIATA');INSERT INTO "${s}".pontos_de_acesso VALUES('${porta}','${e}','${v}','ATIVO')`)});afterAll(async()=>{await p.query(`DROP SCHEMA IF EXISTS "${s}" CASCADE`);await p.end()});it('clona agregado e rollback restaura versão e porta',async()=>{const nova=randomUUID(),r=new RepositorioDoCicloDeVidaPostgresql(p,s);const c=await r.criarCorrecao({negocioId:n,experienciaId:e,novaVersaoId:nova,utilizadorId:u});expect(c?.versaoAnteriorId).toBe(v);const cont=await p.query(`SELECT count(*) n FROM "${s}".blocos WHERE versao_da_experiencia_id=$1`,[nova]);expect(Number(cont.rows[0].n)).toBe(1);const falha=new RepositorioDoCicloDeVidaPostgresql(p,s,async()=>{throw Error('FALHA_INDUZIDA')});await expect(falha.publicarCorrecaoAtomica({negocioId:n,experienciaId:e,novaVersaoId:nova,versaoAnteriorId:v,soma:'x'.repeat(64),agora:new Date()})).rejects.toThrow('FALHA_INDUZIDA');const q=await p.query(`SELECT versao_publicada_id FROM "${s}".experiencias WHERE id=$1`,[e]);expect(q.rows[0].versao_publicada_id).toBe(v);const pt=await p.query(`SELECT versao_da_experiencia_id FROM "${s}".pontos_de_acesso WHERE id=$1`,[porta]);expect(pt.rows[0].versao_da_experiencia_id).toBe(v)})})
